spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myhotel
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: zhanna1966mama
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

clients:
  description:
    url: localhost:8082/api/description
